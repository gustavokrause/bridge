# Principles

Working principles captured from Gustavo's high-level intent (the "intent library"). Each file = one principle, with **Why** (the case that produced it) and **How to apply**. Copied from the per-project Claude memory; this is the project-visible, version-controllable home.

- [Intent-library method](intent-library-method.md) — capture WHY as principles, lead with cause-level reads, compress recurring intent transfer toward zero
- [Pipeline self-resolves](pipeline-self-resolves.md) — brittle on trivial = dead on heavy; fix the class, never patch one case
- [Token economics span sessions](token-economics-span-sessions.md) — judge cost across the full lifecycle; upfront load OK if later sessions are leaner
- [Instruction-file voice is load-bearing](instruction-file-voice-load-bearing.md) — don't strip voice/grammar when compressing CLAUDE.md; A/B real output, not inspection
